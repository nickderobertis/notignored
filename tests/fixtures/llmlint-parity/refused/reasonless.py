# llmlint: ignore[no_todo_comments]
# a reason may not begin below the brackets: the real check-ignores refuses this
# directive outright, so notignored reports it with no reason at all
TODO_MARKER = "not a comment"
