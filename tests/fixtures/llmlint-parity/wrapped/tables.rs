/*
  llmlint: ignore-file[no_todo_comments] the vendored grammar tables keep upstream's
  own markers, and rewriting one would fork a generated file
*/

/** llmlint: ignore[no_debug_prints] the dump below is what this helper is for,
 * and its output is the only view a maintainer gets of the table
 */
pub fn dump() {
    println!("tables");
}
