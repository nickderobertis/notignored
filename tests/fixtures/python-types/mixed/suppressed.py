# One line, two tools: each directive is reported under the tool whose syntax it is.

import notignored_missing_module  # type: ignore[import-not-found]  # no stubs published  # noqa: F401  # imported for its side effects
