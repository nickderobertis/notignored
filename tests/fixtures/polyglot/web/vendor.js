// @ts-nocheck bundled by a toolchain that predates our types
// biome-ignore-all lint/suspicious/noDebugger: a vendored bundle, not ours to edit
/* eslint-disable no-alert, no-console --
   the bundle is regenerated on every vendor release, so a per-line
   suppression here would be lost with the next drop */
alert("legacy");
console.log("legacy");
debugger;
